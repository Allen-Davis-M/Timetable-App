// Unused for now — not part of the current design's screens (the solver
// doesn't assign rooms yet either, see docs/ARCHITECTURE.md). Kept out of
// the build (not imported anywhere); reintroduce when room assignment is
// built, or delete.
